<% form_for book, url(:books, :destroy, :id => book.id), :'data-confirm' => t('confirm_deletion') do |f| %>
  <%= hidden_form_method_field 'delete' %>
  <%= f.submit t('delete') %>
  <% if f.error_messages.present? %><p><%= f.error_messages %></p><% end %>
<% end %>
