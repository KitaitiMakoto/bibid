<% content_for :title do %><%= t('users.list') %><% end %>

<article>
  <h1><%= t('users.list') %></h1>

  <% @users.each do |user| %>
  <ul>
    <li><%= link_to user.display_name, url(:users, :show, :name => user.name) %></li>
  </ul>
  <% end %>

  <%= paginate @users %>
</article>
